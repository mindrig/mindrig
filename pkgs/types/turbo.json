{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "./src/**/*.type", "./genotype.toml"],
      "outputs": ["./pkgs/**"]
    }
  },
  "extends": ["//"]
}
